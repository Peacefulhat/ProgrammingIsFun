End of Statement:
    Token: ;
    Description: used to terminate a statement.

Comments:
    Token: //
    Description: Single Line Comment.

    Token: /* ... */
    Description: Multi Line Comment.

// Compiled verision
Body / Entry Point:
    Token:  main(){...}
    Description: Entry point of program.

Auto Type Inference:
    Tokens: :=
    Description: Automatically, makes the variable of same type.

Functions:

Function Defination:

FunctionName(Parameters="DefaultValue) -> (return types)
{

}

Function Calling:

FunctionName(args);// Calling with passed values.
FunctionName(); // Calling with default args.


Block:
// must stay inside a function.

{
// Statement
}

Contidionals

if condition {

}

if condition
{

}
else
{

}

// case can be anthing like string, int, floats, (Custom type also)
switch(case)
{

case 1 : {

}
default:

}